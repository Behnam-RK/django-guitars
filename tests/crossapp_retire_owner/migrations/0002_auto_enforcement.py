# Generated by makeguitarmigrations command! [DIGEST:865596acd982155cc7ac91dd46cf19eb]

from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ('crossapp_retire_owner', '0001_initial'),
    ]

    operations = [
        # Soft Delete Rule on "crossapp_retire_owner_retiree" table! [SQL:b8a301f30663]
        migrations.RunSQL(
            sql="""
            CREATE OR REPLACE RULE soft_delete
                AS ON DELETE TO "crossapp_retire_owner_retiree"
                WHERE COALESCE(current_setting('rules.hard_deletion', true), '') <> 'on'
                DO INSTEAD (
                    UPDATE "crossapp_retire_owner_retiree"
                    SET _deleted_at = NOW()
                    WHERE "id" = old."id" AND _deleted_at IS NULL
                );
        """,
            reverse_sql="""
            DROP RULE soft_delete ON "crossapp_retire_owner_retiree";
        """,
        ),

    ]
