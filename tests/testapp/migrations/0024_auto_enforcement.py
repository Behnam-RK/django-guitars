# Generated by makeguitarmigrations command! [DIGEST:60cde03ce2b756c41a84f56569473de9]

from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ('testapp', '0011_auto_enforcement_trigger_function'),
        ('testapp', '0023_merch_featured_orchestra_presskit_and_more'),
    ]

    operations = [
        # Updated at Trigger on "testapp_presskit" table! [SQL:626eb4fa0621]
        migrations.RunSQL(
            sql="""
            CREATE TRIGGER updated_at_trigger
                AFTER UPDATE ON "testapp_presskit" REFERENCING NEW TABLE AS new_table
                FOR EACH STATEMENT
                WHEN (pg_trigger_depth() = 0)
                EXECUTE FUNCTION set_updated_at('id');
        """,
            reverse_sql="""
            DROP TRIGGER updated_at_trigger ON "testapp_presskit";
        """,
        ),

        # Soft Delete Rule on "testapp_presskit" table! [SQL:7308c71677b5]
        migrations.RunSQL(
            sql="""
            CREATE OR REPLACE RULE soft_delete
                AS ON DELETE TO "testapp_presskit"
                WHERE COALESCE(current_setting('rules.hard_deletion', true), '') <> 'on'
                DO INSTEAD (
                    UPDATE "testapp_presskit"
                    SET _deleted_at = NOW()
                    WHERE "id" = old."id" AND _deleted_at IS NULL
                );
        """,
            reverse_sql="""
            DROP RULE soft_delete ON "testapp_presskit";
        """,
        ),

        # Soft Delete Owned Rule on "testapp_presskit" that is owned by "testapp_album" via "alt_press_kit_id"! [SQL:e848f940a603]
        migrations.RunSQL(
            sql="""
            CREATE OR REPLACE RULE "soft_delete_owned_testapp_presskit_alt_press_kit_id"
                AS ON UPDATE TO "testapp_album"
                WHERE old._deleted_at IS NULL AND new._deleted_at IS NOT NULL AND
                      COALESCE(current_setting('rules.hard_deletion', true), '') <> 'on'
                DO ALSO (
                    UPDATE "testapp_presskit"
                    SET _deleted_at = NOW()
                    WHERE "id" = old."alt_press_kit_id"
                      AND _deleted_at IS NULL
                      AND NOT EXISTS (
                          SELECT 1 FROM "testapp_album" AS guitars_owner
                          WHERE guitars_owner."alt_press_kit_id" = old."alt_press_kit_id"
                            AND guitars_owner."id" <> old."id"
                            AND guitars_owner._deleted_at IS NULL
                      )
                );
        """,
            reverse_sql="""
            DROP RULE "soft_delete_owned_testapp_presskit_alt_press_kit_id" ON "testapp_album";
        """,
        ),

        # Soft Delete Owned Rule on "testapp_presskit" that is owned by "testapp_album" via "press_kit_id"! [SQL:10ff7b376ed1]
        migrations.RunSQL(
            sql="""
            CREATE OR REPLACE RULE "soft_delete_owned_testapp_presskit_press_kit_id"
                AS ON UPDATE TO "testapp_album"
                WHERE old._deleted_at IS NULL AND new._deleted_at IS NOT NULL AND
                      COALESCE(current_setting('rules.hard_deletion', true), '') <> 'on'
                DO ALSO (
                    UPDATE "testapp_presskit"
                    SET _deleted_at = NOW()
                    WHERE "id" = old."press_kit_id"
                      AND _deleted_at IS NULL
                      AND NOT EXISTS (
                          SELECT 1 FROM "testapp_album" AS guitars_owner
                          WHERE guitars_owner."press_kit_id" = old."press_kit_id"
                            AND guitars_owner."id" <> old."id"
                            AND guitars_owner._deleted_at IS NULL
                      )
                );
        """,
            reverse_sql="""
            DROP RULE "soft_delete_owned_testapp_presskit_press_kit_id" ON "testapp_album";
        """,
        ),

        # Soft Delete Owned Rule on "testapp_ensemble" that is owned by "testapp_merch" via "featured_orchestra_id"! [SQL:cc54e6aaa037]
        migrations.RunSQL(
            sql="""
            CREATE OR REPLACE RULE "soft_delete_owned_testapp_ensemble_featured_orchestra_id"
                AS ON UPDATE TO "testapp_merch"
                WHERE old._deleted_at IS NULL AND new._deleted_at IS NOT NULL AND
                      COALESCE(current_setting('rules.hard_deletion', true), '') <> 'on'
                DO ALSO (
                    UPDATE "testapp_ensemble"
                    SET _deleted_at = NOW()
                    WHERE "id" = old."featured_orchestra_id"
                      AND _deleted_at IS NULL
                      AND NOT EXISTS (
                          SELECT 1 FROM "testapp_merch" AS guitars_owner
                          WHERE guitars_owner."featured_orchestra_id" = old."featured_orchestra_id"
                            AND guitars_owner."id" <> old."id"
                            AND guitars_owner._deleted_at IS NULL
                      )
                );
        """,
            reverse_sql="""
            DROP RULE "soft_delete_owned_testapp_ensemble_featured_orchestra_id" ON "testapp_merch";
        """,
        ),

    ]
