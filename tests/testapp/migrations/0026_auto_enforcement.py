# Generated by makeguitarmigrations command! [DIGEST:f66533208e576620be83a7fc45e4c1e3]

from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ('testapp', '0011_auto_enforcement_trigger_function'),
        ('testapp', '0025_patron'),
    ]

    operations = [
        # Updated at Trigger on "testapp_presskit" table! [SQL:626eb4fa0621]
        migrations.RunSQL(
            sql="""
            CREATE TRIGGER updated_at_trigger
                AFTER UPDATE ON "testapp_presskit" REFERENCING NEW TABLE AS new_table
                FOR EACH STATEMENT
                WHEN (pg_trigger_depth() = 0)
                EXECUTE FUNCTION set_updated_at('id');
        """,
            reverse_sql="""
            DROP TRIGGER updated_at_trigger ON "testapp_presskit";
        """,
        ),

        # Soft Delete Rule on "testapp_presskit" table! [SQL:7308c71677b5]
        migrations.RunSQL(
            sql="""
            CREATE OR REPLACE RULE soft_delete
                AS ON DELETE TO "testapp_presskit"
                WHERE COALESCE(current_setting('rules.hard_deletion', true), '') <> 'on'
                DO INSTEAD (
                    UPDATE "testapp_presskit"
                    SET _deleted_at = NOW()
                    WHERE "id" = old."id" AND _deleted_at IS NULL
                );
        """,
            reverse_sql="""
            DROP RULE soft_delete ON "testapp_presskit";
        """,
        ),

        # Updated at Trigger on "testapp_patron" table! [SQL:f15a2ba7ba52]
        migrations.RunSQL(
            sql="""
            CREATE TRIGGER updated_at_trigger
                AFTER UPDATE ON "testapp_patron" REFERENCING NEW TABLE AS new_table
                FOR EACH STATEMENT
                WHEN (pg_trigger_depth() = 0)
                EXECUTE FUNCTION set_updated_at('id');
        """,
            reverse_sql="""
            DROP TRIGGER updated_at_trigger ON "testapp_patron";
        """,
        ),

        # Soft Delete Rule on "testapp_patron" table! [SQL:40420f7e7728]
        migrations.RunSQL(
            sql="""
            CREATE OR REPLACE RULE soft_delete
                AS ON DELETE TO "testapp_patron"
                WHERE COALESCE(current_setting('rules.hard_deletion', true), '') <> 'on'
                DO INSTEAD (
                    UPDATE "testapp_patron"
                    SET _deleted_at = NOW()
                    WHERE "id" = old."id" AND _deleted_at IS NULL
                );
        """,
            reverse_sql="""
            DROP RULE soft_delete ON "testapp_patron";
        """,
        ),

        # Soft Delete Owned Rule on "testapp_presskit" that is owned by "testapp_album" via "alt_press_kit_id"! [SQL:c18377a59cad]
        migrations.RunSQL(
            sql="""
            CREATE OR REPLACE RULE "soft_delete_owned_16_testapp_presskit_16_alt_press_kit_id"
                AS ON UPDATE TO "testapp_album"
                WHERE old._deleted_at IS NULL AND new._deleted_at IS NOT NULL AND
                      COALESCE(current_setting('rules.hard_deletion', true), '') <> 'on'
                DO ALSO (
                    UPDATE "testapp_presskit"
                    SET _deleted_at = NOW()
                    WHERE "id" = old."alt_press_kit_id"
                      AND _deleted_at IS NULL
                      AND NOT EXISTS (
                          SELECT 1 FROM "testapp_album" AS guitars_owner
                          WHERE guitars_owner."alt_press_kit_id" = old."alt_press_kit_id"
                            AND guitars_owner."id" <> old."id"
                            AND guitars_owner._deleted_at IS NULL
                      )
                );
        """,
            reverse_sql="""
            DROP RULE "soft_delete_owned_16_testapp_presskit_16_alt_press_kit_id" ON "testapp_album";
        """,
        ),

        # Soft Delete Owned Rule on "testapp_presskit" that is owned by "testapp_album" via "press_kit_id"! [SQL:e2f060af31c4]
        migrations.RunSQL(
            sql="""
            CREATE OR REPLACE RULE "soft_delete_owned_16_testapp_presskit_12_press_kit_id"
                AS ON UPDATE TO "testapp_album"
                WHERE old._deleted_at IS NULL AND new._deleted_at IS NOT NULL AND
                      COALESCE(current_setting('rules.hard_deletion', true), '') <> 'on'
                DO ALSO (
                    UPDATE "testapp_presskit"
                    SET _deleted_at = NOW()
                    WHERE "id" = old."press_kit_id"
                      AND _deleted_at IS NULL
                      AND NOT EXISTS (
                          SELECT 1 FROM "testapp_album" AS guitars_owner
                          WHERE guitars_owner."press_kit_id" = old."press_kit_id"
                            AND guitars_owner."id" <> old."id"
                            AND guitars_owner._deleted_at IS NULL
                      )
                );
        """,
            reverse_sql="""
            DROP RULE "soft_delete_owned_16_testapp_presskit_12_press_kit_id" ON "testapp_album";
        """,
        ),

        # Soft Delete Owned Rule on "testapp_ensemble" that is owned by "testapp_merch" via "featured_orchestra_id"! [SQL:a05f5065aea6]
        migrations.RunSQL(
            sql="""
            CREATE OR REPLACE RULE "soft_delete_owned_16_testapp_ensemble_21_featured_orchestra_id"
                AS ON UPDATE TO "testapp_merch"
                WHERE old._deleted_at IS NULL AND new._deleted_at IS NOT NULL AND
                      COALESCE(current_setting('rules.hard_deletion', true), '') <> 'on'
                DO ALSO (
                    UPDATE "testapp_ensemble"
                    SET _deleted_at = NOW()
                    WHERE "id" = old."featured_orchestra_id"
                      AND _deleted_at IS NULL
                      AND NOT EXISTS (
                          SELECT 1 FROM "testapp_merch" AS guitars_owner
                          WHERE guitars_owner."featured_orchestra_id" = old."featured_orchestra_id"
                            AND guitars_owner."id" <> old."id"
                            AND guitars_owner._deleted_at IS NULL
                      )
                );
        """,
            reverse_sql="""
            DROP RULE "soft_delete_owned_16_testapp_ensemble_21_featured_orchestra_id" ON "testapp_merch";
        """,
        ),

        # Soft Delete Owned Rule on "testapp_ensemble" that is owned by "testapp_patron" via "ensemble_id"! [SQL:a62e3b4b40fb]
        migrations.RunSQL(
            sql="""
            CREATE OR REPLACE RULE "soft_delete_owned_16_testapp_ensemble_11_ensemble_id"
                AS ON UPDATE TO "testapp_patron"
                WHERE old._deleted_at IS NULL AND new._deleted_at IS NOT NULL AND
                      COALESCE(current_setting('rules.hard_deletion', true), '') <> 'on'
                DO ALSO (
                    UPDATE "testapp_ensemble"
                    SET _deleted_at = NOW()
                    WHERE "id" = old."ensemble_id"
                      AND _deleted_at IS NULL
                      AND NOT EXISTS (
                          SELECT 1 FROM "testapp_patron" AS guitars_owner
                          WHERE guitars_owner."ensemble_id" = old."ensemble_id"
                            AND guitars_owner."id" <> old."id"
                            AND guitars_owner._deleted_at IS NULL
                      )
                );
        """,
            reverse_sql="""
            DROP RULE "soft_delete_owned_16_testapp_ensemble_11_ensemble_id" ON "testapp_patron";
        """,
        ),

    ]
